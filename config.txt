# Parametry algorytmu
algorithm         = BB_BEST           # Dostepne: NN, RAND, RNN, RNN_NO_TIES, BF, BB_BFS, BB_DFS, BB_BEST
repetitions       = 10           # Liczba powtorzen dla usrednienia statystyk
randIterations    = 1000000      # Ilosc losowanych sciezek dla alg. RAND
ubStrategy        = RNN          # Strategia UB dla B&B: INF, RAND, NN, RNN, RNN_NO_TIES

# Parametry instancji wejsciowej
generateRandom    = 1            # 1 = wlasny generator, 0 = wczytywanie z pliku
symmetric         = 0            # 1 = graf symetryczny, 0 = graf asymetryczny
inputFile         = data/tsp/matrix_10x10.tsp
outputFile        = results/output.csv

# Pomiary i petla testowa
startInstanceSize = 30            # Rozmiar poczatkowej instancji do zbadania
instancesCount    = 1            # Ilosc badanych rozmiarow (np. 5 oznacza N=11, 12, 13, 14, 15)
showProgress      = 1            # 1 = wyswietlaj pasek postepu w konsoli

minWeight         = 1            # Minimalna waga krawedzi
maxWeight         = 100          # Maksymalna waga krawedzi